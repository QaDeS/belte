<script lang="ts">
import { getCurrent, getScene } from "$lib/babylon"
import { {{name}}{{#each imports}}, {{.}}{{/each}} } from "{{entry}}"

{{>params}}
{{>attach}}

const scene = getScene()

const node = new {{name}}(
    {{#each args}}
        {{name}},
    {{/each}}
)

{{>update}}

node.onRender = () => {
    {{#each updates}}
        {{name}} = node.{{name}}
    {{/each}}
}

</script>

